# Hot 100 Creator Discovery Tool
# Copy this to .env — keys are stored in the app DB, not required here.
# The app has a Settings page to enter API keys via the UI.

# Optional: set these as environment variables instead of via the Settings UI
TUBULAR_API_KEY=
MODASH_API_KEY=
